#include<stdio.h>
int main()
{
  int n,i,arr[100],temp;
  scanf("%d",&n);
  for(i=0;i<n;i++)
  {
     scanf("%d",&arr[i]);
  }
  for(i=0;i<n;i++)
  {
     if(arr[0]<arr[i])
     {
        arr[0]=temp;
        arr[0]=arr[i];
        temp=arr[i];    
     }
  }
  printf("%d",arr[0]);
}
